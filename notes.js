//? querSelector() method

/* 
selectors 
-tag name ("tagName")
-class name (".className")
-id name ("#idName")
-attribute name

*/

//--------8 id-class-qs----------

/*

const el = document.getElementById("username");

why we only use this "el" variable name? we can use any variable name but it is a convention to use "el" for element.

what are the various properties of el?
el.innerText
el.classList
el.id
el.style
el.parentElement
el.children
el.setAttribute("attributeName", "attributeValue")

*/

//-------9 Event Listner-------

//everything after document. are methodds of the document. obj.